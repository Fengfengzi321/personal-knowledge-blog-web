# 2026-06-02 工作日志

## 删除 cards（知识卡片）内容类型

- 删除 `content/cards/` 目录（3 个卡片 + .gitkeep）
- 删除 `src/pages/cards/` 目录
- 更新以下文件，移除所有 card 引用：
  - `src/lib/content-utils.mjs` — 移除 cards 分组、relatedCards/featuredCards 关联键
  - `src/components/Sidebar.astro` — 不再加载 cards 模块
  - `src/pages/posts/index.astro` & `[page].astro` — 移除 card 收集和路由
  - `src/pages/search.astro` — 移除 card 引用
  - `src/pages/tags/index.astro` & `[tag].astro` — 移除 card 引用
  - `src/pages/articles/[slug].astro` & `topics/[slug].astro` — 移除 card 关联
- 统一 `hrefFor()` 简化为 `/${item.type}s/${item.slug}/`

## 首页重新设计（参考 hanchan.vip）

参考 https://hanchan.vip/ 单栏极简风格，完全重写 `src/pages/index.astro`：
- **布局**：单栏居中，max-width 640px
- **Hero 区**：圆形头像 + 站点名 + tagline + 专题标签（圆角边框）+ 总字数统计
- **Recent Posts**：文章列表（标题 + 日期日历图标 + 摘要 2 行截断）
- **Read More**：底部链接到 /posts/
- **色彩**：AstroPaper 色值（#282728 / #71717a / #006CAC / #ECE9E9）
- **字体**：Inter
- 去除了领域网格/知识地图布局、统计面板
- 去除了侧边栏

## 其他微调

- `.hero` 的 `align-items: end` → `start`，使头像框顶部与副标题齐平
- 首页统计面板 `grid-template-columns: repeat(2, 1fr)` 覆盖全局 3 列
- 卡片标签删除，只保留专题和文章统计
